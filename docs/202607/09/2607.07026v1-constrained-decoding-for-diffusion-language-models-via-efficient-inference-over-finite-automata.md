---
title: Constrained Decoding for Diffusion Language Models via Efficient Inference over Finite Automata
title_zh: 通过有限自动机的高效推理实现扩散语言模型的约束解码
authors: "Meihua Dang, Stefano Ermon"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07026v1"
tags: ["query:diffusion-lm"]
score: 9.0
evidence: 扩散语言模型的约束解码
tldr: "扩散语言模型在约束解码时面临挑战，因为它们同时采样多个位置，无法沿用自回归模型从左到右的掩码策略。本文提出一种基于有限自动机的精确算法，将自动机视为图形模型，得到约束后验的可处理表示，实现高效采样。该方法保证约束满足，支持贪心和随机解码，并兼容并行和块状解码。在Dream-7B和LLaDA-8B上，函数调用、规划、Text-to-SQL等任务准确率显著提升，例如BFCL-Live上贪心解码从63.9%升至71.5%，且额外开销低于5%。这是首个为扩散模型提供高效约束解码的方案，通过深度缩减使采样深度从线性降为对数。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07026v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 303, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07026v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 623, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07026v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 457, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07026v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 728, \"height\": 325, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07026v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1373, \"height\": 746, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 592, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1123, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1437, \"height\": 718, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1459, \"height\": 1985, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1464, \"height\": 1483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1488, \"height\": 597, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1333, \"height\": 1361, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07026v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1371, \"height\": 970, \"label\": \"Table\"}]"
motivation: 现有约束解码系统仅适用于自回归模型，无法处理扩散语言模型同时采样多位置的特性，亟需新的算法。
method: 将有限自动机视为图形模型，推导约束后验的因子图表示，结合算术电路深度优化实现精确且高效的采样。
result: "在Dream-7B和LLaDA-8B上，函数调用等任务准确率提升5-46个百分点，墙钟开销增加不到5%。"
conclusion: 首次为扩散语言模型提供了精确且高效的约束解码算法，支持并行解码，显著提升生成结构合规性。
---

## 摘要
约束解码对于服务大语言模型至关重要，确保生成的输出遵循特定结构，如JSON模式的函数调用。现有系统专为自回归模型设计，假设从左到右生成，每一步屏蔽无效的下一个词元。然而，扩散语言模型打破了这一假设：它们在每个去噪步骤中从完全分解的平均场分布中同时采样多个位置。在本文中，我们提出了一种精确且可处理的算法，用于从任何可表示为有限自动机的约束下的约束平均场后验中采样。将有限自动机视为图模型，我们获得了约束分布的可处理表示，从而实现高效推理。该方法通过构造保证约束满足，支持贪婪解码和基于采样的解码，并在任意重掩码调度下兼容并行和块状解码。应用算术电路理论中的深度缩减技术，我们进一步将采样深度从序列长度的线性减少为对数级。在Dream-7B和LLaDA-8B上的实证评估显示，在各种任务中均有显著的精度提升，包括函数调用（xLAM、BFCL）、规划（数独、倒计时）、文本到SQL（Spider）和数学推理（GSM-Symbolic），且相对于无约束解码的推理开销很小。例如，在BFCL-Live上，我们的方法将Dream-7B的贪婪解码精度从63.9%提升至71.5%，随机采样精度从22.3%提升至69.0%（而无约束基线崩溃），且墙上时间开销低于5%。

## Abstract
Constrained decoding is essential for serving LLMs, ensuring that generated outputs follow specific structures such as JSON schema-formatted function calls. Existing systems are designed for autoregressive models and assume left-to-right generation, masking out invalid next tokens at each step. Diffusion language models, however, break this assumption: they sample multiple positions simultaneously from a fully-factorized mean-field distribution at each denoising step. In this paper, we present an exact and tractable algorithm for sampling from the constrained mean-field posterior under any constraint expressible as a finite automaton. Viewing finite automata as graphical models, we obtain tractable representations of the constrained distribution that enable efficient inference. The approach guarantees constraint satisfaction by construction, supports both greedy and sampling-based decoding, and is compatible with parallel and block-wise decoding under arbitrary remasking schedules. Applying depth-reduction techniques from arithmetic circuit theory, we further reduce sampling depth from linear to logarithmic in the sequence length. Empirical evaluations on Dream-7B and LLaDA-8B show substantial accuracy gains across various tasks including function calling (xLAM, BFCL), planning (Sudoku, Countdown), text-to-SQL (Spider), and math reasoning (GSM-Symbolic), with little inference overhead relative to unconstrained decoding. For example, on BFCL-Live, our approach improves Dream-7B's greedy decoding accuracy from 63.9% to 71.5%, and stochastic sampling accuracy from 22.3% to 69.0%, where the unconstrained baseline collapses, with under 5% wall-clock overhead.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的详细总结：

# 论文详细总结：通过有限自动机的高效推理实现扩散语言模型的约束解码

## 一、论文的核心问题与整体含义（研究动机和背景）

*   **研究背景：** 大型语言模型在函数调用、SQL生成等场景中，必须严格遵守结构性约束。现有的高效约束解码系统（如XGrammar, SGLang）专为自回归模型设计，核心假设是“从左到右”的逐词生成，每一步可以屏蔽掉违反约束的下一个词元。
*   **核心问题：** 扩散语言模型打破了自回归的假设。它们在每个去噪步骤中，从一个完全分解（因子化）的平均场分布中**同时采样多个位置**。这使得传统的局部掩码策略失效：仅仅在各位置独立地屏蔽无效词元是不够的，因为局部有效的词元组合后可能产生违反全局约束的无效序列。
*   **整体含义：** 本文是首个为扩散语言模型提出精确、可处理且高效的约束解码算法的工作。它解决了扩散模型在需要结构化输出的应用场景中的关键短板，极大地提升了其可靠性和实用性。

## 二、论文提出的方法论

*   **核心思想：** 将有限自动机视为一个图模型，并将其与扩散模型的平均场预测相结合，从而得到约束后验分布 `pθ (x0 | xt, C)` 的一个可处理的因子图表示。
*   **关键技术细节：**
    1.  **问题建模：** 在每个去噪步骤，目标是从约束后验 `pθ (x0 | xt, C)` 中采样 `x̂0`，该后验是模型平均场预测 `∏ pθ (x0ᵢ | xt)` 与约束指示函数 `1[x0 ∈ C]` 的乘积。
    2.  **约束表示：** 将约束 C 编码为一个有限自动机。该自动机可以被视为一个链式图模型，其分布的支撑集恰好是约束语言C。如果自动机是确定性的，则该分布在其支撑集上是均匀的。
    3.  **可处理乘积构造：** 将扩散模型的因子化平均场分布与约束对应的图模型（链结构）的发射因子相乘，得到一个**新的、也是链式结构**的图模型。这个新模型的分布正比于原始约束后验分布。由于图模型是链式的，可以使用标准的**前向-后向消息传递**算法进行精确推理和采样。
    4.  **高效对数深度树采样：** 标准的链式图模型采样需要线性时间（O(L)）。本文借鉴算术电路深度缩减技术，利用**条件独立性**（给定边界状态，中间状态和两边的子序列条件独立），设计了一个**分治算法**。该算法通过自底向上计算多步转移概率，再自顶向下递归采样中间状态，将**采样深度从O(L)降低至O(log L)**，极大提升了GPU上的并行效率。
    5.  **重掩码置信度：** 在扩散模型的每一步确定哪些位置要“提交”（commit）时，论文提出使用**约束边际概率** `pθ (x0ᵢ = x̂ᵢ | xt, C)` 作为置信度信号，而不是无约束的边际概率。这能更准确地反映模型和约束共同对某个位置的偏好。

*   **公式与算法流程（文字说明）：**
    *   **输入：** 当前噪声状态 `xt`，模型预测 `pθ (x0 | xt)`，约束 `C`（表示为有限自动机）。
    *   **步骤1（消息计算 - 自底向上）：** 并行计算所有长度为 `2^k` 的区间的多步转移概率 `p(z_{i+2^k} | z_i)`。
    *   **步骤2（采样 - 自顶向下）:** 
        1.  从边界状态 `(z1, z_{L+1})` 的联合边际分布中采样。
        2.  递归：给定边界状态 `(zℓ, zr)`，计算中点状态 `zm` 的条件分布 `p(zm | zℓ, zr)` 并采样 `zm`。
        3.  将原问题分为左 (ℓ, m) 和右 (m, r) 两个独立子问题，并行递归处理。
    *   **步骤3（输出）：** 当子问题长度小于等于1时，直接采样词元 `xℓ`。

## 三、实验设计

*   **数据集/场景：** 覆盖五大类任务，共六个数据集，具有广泛代表性：
    *   **函数调用：** xLAM, BFCL（含Live和Non-Live子集），评估JSON和Python两种格式。
    *   **规划：** 4x4数独（Sudoku），倒计时（Countdown）。
    *   **代码与数学：** 文本到SQL（Spider），数学推理（GSM-Symbolic）。
*   **Benchmark与基线：** 
    *   **模型：** 选用两个主流开源扩散语言模型：**Dream-7B** 和 **LLaDA-8B**（包括Base和Instruct变体）。
    *   **对比方法：** 主要对比**无约束基线**（Base），即在相同模型、相同配置下不施加任何约束的解码结果。在消融实验中对比了使用不同置信度信号（平均场 vs 约束边际概率）的变体，并与链式（线性的）和树式（对数深度的）两种采样实现进行对比。此外，还与**相同规模的自回归模型**（Llama-3.1-8B, Qwen3-8B）进行了比较。

## 四、资源与算力

*   **明确说明的资源：** 论文仅在运行时（Latency）和吞吐量（Throughput）测量中明确提到了使用 **NVIDIA RTX A6000 GPU**。实验主要是推理评估，未涉及训练过程。
*   **未说明：** 论文**未明确说明**训练任何自定义模型所需的算力（如GPU数量、训练时长）。其方法本身不涉及模型训练，仅在已有模型上进行推理。

## 五、实验数量与充分性

*   **实验数量：** 实验数量充分，包括：
    *   **主实验结果（表1）：** 在10种任务/格式组合下，对2个模型，分别在2种解码模式（贪心、随机）下进行对比，共约40个实验点。
    *   **消融实验（表2）：** 对比了两种重掩码策略（Mf vs Mar）。
    *   **效率分析（表3、图3）：** 对比了链式和树式采样的运行时，以及不同batch size下的吞吐量。
    *   **鲁棒性分析（图4）：** 探讨了模型在不同去噪步数下的性能变化。
    *   **与自回归模型对比（表4）：** 与同等规模的流行自回归模型进行比较。
*   **充分性与公平性：**
    *   实验覆盖了结构化生成的多个关键领域，任务多样性好。
    *   对比的基线是广泛使用的无约束基线，并提供了约束满足率（CS）指标，使得比较合理、公平。
    *   消融实验清晰地分离了“过滤无效序列”和“重排解码顺序”两个贡献。
    *   与自回归模型的对比客观地展示了扩散模型+本方法后与主流模型的差距缩小情况。

## 六、论文的主要结论与发现

1.  **结构化输出性能显著提升：** 在几乎所有基准测试上，本方法在贪心解码和随机采样两种模式下均显著提升了准确率。例如，在BFCL-Live上，Dream-7B的贪心准确率从63.9%提升至71.5%。
2.  **随机采样下恢复性能：** 无约束扩散模型在随机采样（温度=1）下准确率会剧烈下降（例如BFCL-Live从63.9%跌至22.3%）。本方法在随机采样下表现稳健（69.0%），几乎恢复到贪心解码的水平，表明其解决了扩散模型随机生成时结构失效的痛点。
3.  **格式差异可被弥补：** 模型在Python格式和JSON格式上的表现存在显著差异，这种差异源于对格式的生疏而非知识不足。本方法通过强制格式正确性，基本消除了这种差异，性能趋于一致。
4.  **低推理开销：** 在贪心解码下，应用优化的树式采样算法，墙上时间开销仅增加约**4%**，实现了很高的效率和性能增益。
5.  **鲁棒性：** 在仅有少量去噪步数的情况下，本方法的性能依然稳健，而无约束基线则严重恶化。

## 七、优点（亮点）

*   **方法层面：**
    *   **零次成本（0-shot）适配：** 无需微调模型，直接在推理时施加约束。
    *   **理论精确性：** 通过构建可处理的图模型，实现了从约束后验中精确采样（非近似或拒绝式采样），**100%保证约束满足**。
    *   **高效并行化：** 提出对数深度树采样，极大提升了GPU利用率，使计算开销在实际中可忽略不计。
    *   **广泛兼容性：** 支持确定性有限自动机（DFA）和非确定性有限自动机（NFA），兼容多种重掩码调度策略。
*   **实验层面：**
    *   **任务覆盖广：** 覆盖函数调用、规划、代码生成、数学推理等多领域，论证了方法的通用性。
    *   **指标严格：** 采用精确匹配（Exact Match）和执行准确率（Execution Accuracy）等严格指标。
    *   **分析深入：** 不仅有主实验结果，还深入分析了格式差异、去噪步数影响、重掩码策略和运行时开销，论证充分。

## 八、不足与局限

*   **表达力限制：** 方法仅适用于可以用**有限自动机**表达的约束。对于更复杂的约束，如**上下文无关文法**（例如括号完美匹配、嵌套JSON结构），本方法无法直接处理。论文也明确指出这是其局限性。
*   **实验覆盖偏差：** 
    *   **Sudoku约束：** 在数独任务中，DFA只强制了输出格式并固定了预填数字，但并未编码数独的**行、列和宫的不重复约束**。任务的成功依赖于模型本身的推理能力。这稍微稀释了消融实验的纯度，但论文也指出了这一点，表明其专注于语法层约束。
    *   **Spider约束：** SQL任务中使用了大规模NFA，但仅约束了SQL语法和模式引用，未能编码查询结果的语义正确性。可能仍有部分语法正确但语义错误的生成未被完全排除。
*   **风险考量：** 论文在“社会和影响”部分指出，该方法可能被滥用，用于生成语法上看似合法但实际上是有害的工具调用或数据库查询。

**（完）**
