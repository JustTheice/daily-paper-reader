---
title: "SLIM-RL: Risk-Budgeted Random-Masking RL for Diffusion LLMs Without Trajectory Slicing"
title_zh: "SLIM-RL: 无轨迹切分的风险预算随机掩码扩散大语言模型强化学习"
authors: "Ruikang Zhao, Zhenting Wang, Han Gao, Ligong Han"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00208v1"
tags: ["query:diffusion-lm"]
score: 9.0
evidence: 面向扩散大语言模型的强化学习
tldr: 现有扩散LLM强化学习方法TraceRL依赖轨迹切片对齐推理轨迹，训练成本随块大小线性增长。SLIM-RL提出基于风险预算的随机掩码RL，通过τ-budget解码器控制每步提交风险，并采用无迹随机掩码目标结合序列级重要性采样与确定性求积调度。在SDAR-4B上，SLIM-RL仅用0.46倍TraceRL训练样本即达到其最佳MATH500性能，在数学和代码任务上均显著提升。该方法无需重构推理轨迹，τ-budget解码器可零成本迁移至其他扩散LLM，为高效RL训练提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00208v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00208v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1418, \"height\": 835, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00208v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 878, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00208v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1321, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00208v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1407, \"height\": 566, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00208v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 796, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00208v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 754, \"height\": 773, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00208v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1101, \"height\": 325, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00208v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1296, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00208v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1382, \"height\": 888, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00208v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1080, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00208v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1116, \"height\": 1262, \"label\": \"Table\"}]"
motivation: 消除轨迹切片的高训练成本，解决随机掩码与推理轨迹的不匹配问题。
method: 引入τ-budget解码器控制rollout提交风险，结合无迹随机掩码目标与方差缩减技术。
result: "SLIM-RL在MATH500上超TraceRL 6.32%，GSM8K超11.05%，代码任务超约4%，且训练样本减半。"
conclusion: τ-budget解码器可无训练迁移，方法开源，验证了风险预算策略的有效性。
---

## 摘要
针对扩散大语言模型（dLLMs）的强化学习已主要转向轨迹感知方法。当前最先进方法TraceRL认为随机掩码与模型推理轨迹不匹配，并通过将每次rollout切分为最多K/s个轨迹对齐的训练样本来在训练期间重建该轨迹，其成本随块大小K增长。我们证明这种不匹配可以在不重建轨迹的情况下得到缓解。我们的方法SLIM-RL使用tau预算解码器限制每次rollout步骤的提交风险，从而降低训练数据中的总体提交风险。优化过程中，SLIM-RL在这些风险可控的rollout上采用无迹随机掩码目标，该目标自适应方差缩减工具，结合序列级重要性采样以及在我们引入的均值保持、单调递减的每块掩码调度下对掩码级别进行确定性正交化。在SDAR-4B上，SLIM-RL在块大小16时仅使用0.46倍于TraceRL的训练样本即达到其最佳MATH500准确率，在匹配动态采样下，在MATH500上比TraceRL提升6.32%，在GSM8K上提升11.05%。在块大小4时，4B的SLIM-RL在数学任务上超越更大的LLaDA-8B和Dream-7B dLLMs，在MATH500上超过LLaDA-8B 10.76%，同时低于自回归的Qwen2.5-7B。在代码任务上，它在MBPP上比TraceRL提升4.20%，在HumanEval上提升3.65%。tau预算解码器可无训练地跨LLaDA、Dream和SDAR迁移。源代码见 https://github.com/laolaorkkkkk/SLIM-RL 。

## Abstract
Reinforcement learning for diffusion large language models (dLLMs) has largely moved to trajectory-aware methods. The current state of the art, TraceRL, holds that random masking is mismatched with the model's inference trajectory, and it reconstructs that trajectory during training by slicing each rollout into up to K/s trajectory-aligned training samples, a cost that grows with the block size K. We show that this mismatch can be mitigated without reconstructing the trajectory. Our method, SLIM-RL, bounds the commit risk of each rollout step with a tau-budget decoder, reducing aggregate commit risk in the training data. During optimization, SLIM-RL trains on these risk-controlled rollouts with a trace-free random-masking objective that adapts variance-reduction tools, combining sequence-level importance sampling, deterministic quadrature over masking levels under a mean-preserving, monotonically decreasing per-block mask schedule that we introduce. On SDAR-4B, SLIM-RL matches TraceRL's best MATH500 accuracy on only 0.46x its training samples at block size 16, improving over TraceRL by 6.32% on MATH500 and 11.05% on GSM8K under matched dynamic sampling. At block size 4, the 4B SLIM-RL surpasses the larger LLaDA-8B and Dream-7B dLLMs on math, exceeding LLaDA-8B by 10.76% on MATH500 while staying below the autoregressive Qwen2.5-7B. On code, it improves over TraceRL by 4.20% on MBPP and 3.65% on HumanEval. The tau-budget decoder transfers training-free across LLaDA, Dream, and SDAR. The source code is available at https://github.com/laolaorkkkkk/SLIM-RL .

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：扩散大语言模型（dLLMs）通过迭代去噪生成文本，块级变体（如SDAR）在块内并行去噪、块间自回归，兼顾了并行性与KV缓存复用。近年来，强化学习（RL）被广泛用于提升dLLMs的推理能力。
- **核心问题**：现有最先进的轨迹感知RL方法TraceRL认为随机掩码与模型推理轨迹不匹配，因此在训练中通过将每个rollout切片为最多⌈K/s⌉个轨迹对齐样本（K为块大小，s为收缩因子）来重建精确轨迹。这一做法带来了显著的训练成本——切片数量随K线性增长，且需在轨迹保真度与成本之间权衡。作者的核心问题是：是否必须精确重建推理轨迹才能达到同等性能？能否通过其他方式缓解随机掩码与轨迹的不匹配，实现无轨迹切分的RL训练？
- **整体含义**：本文证明了在不重建轨迹的前提下，通过控制rollout生成阶段的提交风险（commit risk）和优化阶段的梯度方差，可以使无迹随机掩码RL与轨迹感知方法性能相当甚至更优。这为dLLM RL提供了更高效、更简洁的训练范式，降低了计算开销和数据需求。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
SLIM-RL将dLLM RL分为两个独立阶段：**风险可控的rollout收集** 与 **无迹低方差优化**，通过三个关键组件协同工作。

### 关键技术细节

1. **τ-budget动态解码器（τ-Budget Dynamic Unmasking）**  
   - 用于rollout生成和测试时推理，无需训练。  
   - 在每个去噪步骤s，首先筛选出置信度pi > τ的候选位置集As。  
   - 定义每个候选的置信度不确定性ui = 1 - pi，并设定步骤预算Bs = m(1-τ)，其中m≥1。  
   - 将候选按ui升序排序，提交最大前缀使其累计不确定性≤Bs。  
   - 直观解释：预算Bs是对期望错误提交次数（E[WrongCommit]）的校准代理，从而限制每步的总体风险。  
   - 与动态采样（点态阈值）相比，τ-budget避免了多个刚过阈值位置同时提交导致的累积风险。

2. **无迹随机掩码训练目标（Trace-Free Random-Masking Objective）**  
   - **序列级重要性比率**：代替token级比率，对整条响应在给定掩码水平t下计算归一化的去噪对数分数ℓjθ(t)，然后将其指数化得到序列级比率ρ(t)j(θ)。这降低了响应长度引入的高方差。  
   - **确定性正交化**：对掩码水平t的期望进行Gauss-Legendre求积（Q=3个节点），代替随机采样，消除t随机性带来的主要方差来源。  
   - **未归一化优势**：使用Aj = rj - (1/G)∑ri，避免归一化引入的偏差。

3. **均值保持递减的逐块掩码调度**：  
   - 将全局掩码水平t重新分配为各块的掩码率p1 ≥ p2 ≥ ... ≥ pB，均值仍为t。  
   - 使用余弦调度：pb = t + (δ/2) cos(π ξb)，其中ξb为归一化块位置。  
   - 作用：将策略梯度信号集中到最早、条件最强的块上，因为这些块影响后续所有块，从而提高学习效率。

### 算法流程（文字说明）
- 每个外循环步骤，从策略πθold中采样G个响应，使用τ-budget解码器生成rollout。  
- 基于奖励计算未归一化优势Aj。  
- 对每个响应和每个求积节点tq，构建随机掩码版本的响应，计算序列级去噪对数分数和比率。  
- 优化目标：JSLIM(θ) = E[ (1/G) ∑j ∑q ωq ψϵ(ρ(tq)j(θ), Aj) - β DKL(πθ∥πθold) ]，其中ψϵ为裁剪策略目标。

## 3. 实验设计

### 使用的数据集/场景
- **训练数据**：MATH数据集（级别3-5）和PrimeIntellect-verified编程问题，按任务独立训练模型。  
- **评估基准**：  
  - 数学：MATH500、GSM8K  
  - 代码：MBPP、HumanEval  
- **解码器**：动态采样（τ=0.9）和τ-budget解码器（m=1），测试时可互换。

### 对比方法
- **自回归参考**：Qwen2.5-7B-Instruct  
- **全注意力dLLMs**：LLaDA-8B-Instruct、Dream-7B-Instruct  
- **RL基线**：  
  - Random-masking RL（Zhao et al. 2026）  
  - TraceRL（Wang et al. 2025b）——当前最先进的轨迹感知方法  
- **本方法**：SLIM-RL（ours）

### 实验规模
- 基模型：SDAR-4B-Chat（块大小16和4）、SDAR-1.7B-Chat（块大小4）  
- 训练步数：SDAR-4B块大小16训练160步，块大小4训练100步；1.7B训练至收敛；代码RL从4B数学模型继续训练至收敛。  
- 每步采样128个prompt，每个prompt生成G=8个响应（温度1.0）。  
- 求积节点Q=3，学习率1e-6，clip ϵ=0.1，KL系数β=0.01。

## 4. 资源与算力

- **GPU配置**：8×A100（40 GB），分布在两个节点，每个节点4块GPU。  
- **训练时长**：文中对整体训练时长未明确给出具体数字，但提到SDAR-4B块大小16训练160步，块大小4训练100步，使用上述硬件配置。1.7B模型训练至收敛。  
- **混合精度**：bf16（TF32启用）。  
- **总计算量**：未以FLOPs或GPU小时数形式报告，但提供了相对数据效率（如0.46倍训练样本达到TraceRL最佳）。

## 5. 实验数量与充分性

### 实验组数
- **主实验**：表1报告了在MATH500、GSM8K、MBPP、HumanEval四个基准上，使用动态采样和τ-budget两种解码器的完整对比，覆盖了4B块大小16（数学RL）、4B块大小4（数学RL+代码RL）、1.7B块大小4（数学RL）等多种设置。  
- **消融实验**：表2针对MATH500进行了6项leave-one-out消融：重要性比率粒度、rollout解码器、逐块掩码调度、求积节点数、以及对TraceRL使用τ-budget rollouts的效果。  
- **块大小扩展实验**：表3对比了块大小16和4下SLIM-RL与TraceRL的性能差距。  
- **跨架构迁移**：表4在LLaDA-8B、Dream-7B和SDAR-4B上评估τ-budget解码器的零成本迁移效果。  
- **训练动态**：图2-4显示了MATH500准确率随训练样本变化、1.7B训练动态、TPF/生成长度/掩码比率变化。  
- **数据效率**：表6给出了不同设置下达到TraceRL最佳准确率所需相对数据量。

### 充分性评价
- **正面**：实验设计较为全面，覆盖了不同模型规模（4B、1.7B）、不同块大小（16、4）、不同任务类型（数学、代码），并与多个基线（包括自回归、全注意力dLLM、轨迹感知RL、随机掩码RL）进行了公平对比。解码器作为独立因素也进行了控制（测试时动态采样与τ-budget互换）。消融实验验证了每个组件的贡献。
- **客观性**：评估指标采用准确率（%），代码任务要求所有测试通过。采样多次取平均（n=9或n=3）。基线使用官方或公认设置。  
- **潜在不足**：  
  - 训练数据仅使用MATH和编程问题，未在其他领域（如常识推理、多模态）验证。  
  - 所有实验基于SDAR模型，虽然τ-budget解码器跨架构测试，但完整的SLIM-RL算法（包括训练）仅对SDAR进行，通用性有待证明。  
  - 未报告多次运行的标准差或置信区间，可能影响对统计显著性的判断。  
  - 训练时间的具体GPU小时数缺失，不利于计算效率的精确比较。

## 6. 主要结论与发现

- **轨迹重建不是必需的**：无迹随机掩码RL通过风险预算和方差缩减，可以匹配甚至超越轨迹感知RL。  
- **性能提升**：  
  - 在SDAR-4B块大小16上，SLIM-RL在MATH500上比TraceRL高6.32%，GSM8K高11.05%。  
  - 仅用0.46倍训练样本即达到TraceRL最佳MATH500准确率。  
  - 块大小4时，4B的SLIM-RL在数学上超越更大的LLaDA-8B和Dream-7B，在代码上比TraceRL提升4-5%。  
- **τ-budget解码器效果**：有效降低每步期望错误提交数量（图5），且可无训练迁移至LLaDA、Dream和SDAR。  
- **组件贡献**：确定性求积贡献最大（26.56→32.91），其次是rollout解码器（27.09）、递减掩码调度（27.00）、序列级比率（28.09）。τ-budget rollouts对TraceRL无效（反而下降），说明其收益来自与无迹训练的协同。

## 7. 优点

- **创新性**：首次展示轨迹重建并非dLLM RL的必要条件，提出了基于风险预算的新范式。  
- **简洁高效**：无需切片，训练成本与块大小无关（恒定Q=3），数据效率显著提升。  
- **实用性强**：τ-budget解码器无需训练即可直接替换动态采样，适用于多种dLLM架构，下游用户可直接受益。  
- **方法论扎实**：结合序列级比率、确定性求积、未归一化优势等方差缩减技术，并引入掩码调度，消融实验清晰验证了各组件贡献。  
- **实验结果有力**：在四个基准上均优于或持平当前最佳方法，且跨规模、跨块大小一致表现。

## 8. 不足与局限

- **实验覆盖有限**：仅基于SDAR模型训练SLIM-RL，未在LLaDA、Dream等全注意力dLLMs上验证完整的训练流程（仅测试了解码器迁移）。模型通用性存疑。  
- **任务多样性不足**：仅考察数学和代码，未涉及更广泛的NLP任务（如问答、摘要、对话）或多模态场景。  
- **未报告方差**：缺乏多次运行的统计显著性分析，结果可能存在随机波动。  
- **训练细节缺失**：未提供精确的GPU训练总时间或步数对应的实际挂钟时间，不利于计算成本对比。  
- **超参数敏感度未探索**：τ、m、求积节点数Q等对性能的影响仅部分展示（如Q=2 vs. Q=3），未进行系统调优分析。  
- **长响应场景未验证**：当前响应长度固定256，对于需要更长文本的任务（如代码生成、长对话），TPF和性能可能变化。  
- **理论分析有限**：虽然给出了τ-budget的期望错误提交近似校准，但未提供严格的收敛性证明或方差界。

（完）
